To run the application, open the terminal from the root folder,
and enter the command "docker-compose up". wait for docker containers to start.
After that, you can go to "http://localhost:4200/" and use the app to work.

Username and password for admin: "admin"
Username and password for the user: "user"